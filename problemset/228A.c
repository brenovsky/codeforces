#include <stdio.h>

int main()
{
    long int colors[4];
    long int aux[4];
    int i, j;
    int count = 0;
    int same_color;

    for (i = 0; i < 4; i++) {
        scanf("%ld", &colors[i]);
    }

    aux[0] = colors[0];

    for (i = 1; i < 4; i++) {

        aux[i] = colors[i];
        for (j = 0; j < 4; i++) {
            if (colors) //if ... in ... (python)
        }
    }

    return 0;
}
